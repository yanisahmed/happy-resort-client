import React from 'react';
import NotFound from '../../component/NotFound/NotFound'

const NotFoundPage = () => {
    return (
        <>
            <NotFound></NotFound>
        </>
    );
};

export default NotFoundPage;